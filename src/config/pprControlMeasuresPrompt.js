"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.PPR_CONTROL_MEASURES_SYSTEM_PROMPT = void 0;
exports.buildControlMeasuresUserPrompt = buildControlMeasuresUserPrompt;
const pprTextPrioritize_1 = require("../lib/pprTextPrioritize");
/** Извлечение данных НДПР, мер контроля и PDF-документа одним ответом Claude/Gemini. */
exports.PPR_CONTROL_MEASURES_SYSTEM_PROMPT = `Ты — эксперт по промышленной безопасности ТОО «Урал Ойл энд Газ» (UOG).
По программе производства работ (ППР) извлеки данные для наряда-допуска и меры контроля для АСОР.

Ответ — ТОЛЬКО валидный JSON без markdown-обёртки:
{
  "workTitle": "наименование работ (суть, без префикса ППР)",
  "workTasks": [
    {
      "taskTitle": "название этапа без номера 3.1",
      "workContent": "полное описание этапа: операции, объём, охват, последовательность, оборудование на этапе"
    }
  ],
  "toolsAndEquipment": "инструменты и оборудование из ППР (перечень через ; или запятую)",
  "items": [
    {
      "section": "раздел документа",
      "hazard": "опасный фактор",
      "controlMeasures": ["мера 1", "мера 2"]
    }
  ],
  "pdfDocument": null
}

Правила workTitle:
- Из шапки документа: только суть работ после «…РАБОТ ПО» — без «ПЛАН ОРГАНИЗАЦИИ», «ПРОГРАММА ПРОИЗВОДСТВА РАБОТ».
- Обычный регистр (не КАПС). «ПО ОЧИСТКЕ» → «Очистка».
- Пример: «Очистка трубопровода GRE от скважины U12 до сборной станции».

Правила workTasks (главный блок — этапы работ):
- Из разделов 2–3 ППР, «Этапы выполнения», «Описание технологических операций», подразделов 3.1, 3.2…
- Каждый этап — отдельный объект: taskTitle (краткое название БЕЗ нумерации 3.1) + workContent (развёрнутое описание).
- В workContent включай: что делается, объём/охват этапа, ключевые операции, оборудование этапа, последовательность.
- НЕ разделяй на отдельные «описание работ» и «объём» — всё по этапам в workTasks.
- Минимум столько этапов, сколько в ППР (обычно 5–8 для технологических работ).
- Не выдумывай этапы, которых нет в документе.

Правила toolsAndEquipment:
- Перечисли оборудование и инструменты из раздела «Инструменты и оборудование», технологических операций (3.x) и «Техника безопасности».
- Включай очистные устройства, азотные установки, сепараторы, арматуру, шланги, фитинги, манометры, заглушки, газоанализаторы, ЛАРН и т.п., если они упомянуты в документе.

Правила items:
- Меры из разделов «Техника безопасности», «Опасные зоны», технологических подразделов.
- До 10 блоков, каждая мера — отдельная строка.

Правила pdfDocument:
- Не заполняй pdfDocument — оставь null (PDF соберётся отдельно из items).`;
function buildControlMeasuresUserPrompt(docText, fileName) {
    const trimmed = (0, pprTextPrioritize_1.prioritizeSafetyText)(docText, 14000);
    return `Файл: ${fileName}

Текст документа ППР:
---
${trimmed}
---

Извлеки workTasks (этапы с описаниями по пунктам), toolsAndEquipment и items в JSON по схеме из системного промпта. pdfDocument оставь null.`;
}
//# sourceMappingURL=pprControlMeasuresPrompt.js.map